# Copy this file to android/keystore.properties and replace the values below.
# Keep the real file private. It is gitignored.

storeFile=keystore/go-mission-upload.jks
storePassword=replace-with-your-keystore-password
keyAlias=go-mission-upload
keyPassword=replace-with-your-key-password
